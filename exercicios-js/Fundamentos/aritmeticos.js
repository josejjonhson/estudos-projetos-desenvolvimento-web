const [a, b, c, d] = [3, 5, 1, 15] // Forma reduzida de criar quatro variáveis utilizando o operador destructing;

const soma = a + b + c + d // Operação binária;
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma)
console.log(subtracao)
console.log(multiplicacao)
console.log(divisao)
console.log(modulo)

console.log(-soma) // Operador unário convertendo a soma para negativo;