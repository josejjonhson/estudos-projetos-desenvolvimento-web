// Dando uma olhada na API Math do js que nos dá praticidade na hora de utilizar a matemática;
const raio = 5.6
const area = Math.PI * Math.pow(raio, 2)

/**
 * "Math.PI" nos retornar diretamente o valor de PI;
 * "Math.pow" nos permite realizar a potenciação.
 */

console.log(area.toFixed(2))
