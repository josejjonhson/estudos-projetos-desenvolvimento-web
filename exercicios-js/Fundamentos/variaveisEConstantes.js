/**
 * Uma frase que ouvi e foi muito boa para a fixação do conhecimento sobre estruturas de dados foi:
 * "Estrutura de dados não é o dado em sí, más é algo que define as regras que os dados vão seguir."
 */

var a = 3 // Na hora da nomeação das variáveis temos que ser objetivos e claros;
let b = 4

// É interessante evitarmos o uso de "var", "let" é uma forma mais moderna que temos hoje em dia;

var a = 5
//let b = 10
b = 10

console.log(a, b)

/**
 * Quando fazemos a utilização do "var", podemos redeclarar uma variável duas vezes num mesmo bloco de código.
 * De cara já vemos uma diferença entre "let" e "var"; 
 */

const PI = 3.1415 // A constante não pode ser alteradas após a criação ou primeira atribuição;
console.log(PI)

// OBS:. "Quanto mais constante estiver o seu programa, melhor será para mantê-lo."